# 청약 정보 자동화 시스템 - 필수 라이브러리
# 설치 명령어: pip install -r requirements.txt

# 핵심 라이브러리
requests>=2.31.0
pandas>=2.0.0
openpyxl>=3.1.0
lxml>=4.9.0

# 암호화 및 보안
cryptography>=41.0.0

# 스케줄링
schedule>=1.2.0

# 설정 관리
python-dotenv>=1.0.0

# 로깅 개선
colorlog>=6.7.0

# 유틸리티
validators>=0.22.0

# 카카오톡 API (선택사항)
requests-oauthlib>=1.3.0

# 참고: tkinter, smtplib, email, configparser, logging은 Python 표준 라이브러리